import SignalsPage from "./signals/page";

export const dynamic = "force-dynamic";

/**
 * Sprint 1: the homepage IS the signal feed — no login wall, per docs/PRD.md.
 * Sprint 3 replaces this with the operational dashboard.
 */
export default SignalsPage;
